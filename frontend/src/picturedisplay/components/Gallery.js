import React from 'react';
import PictureDisplay from '../containers/PictureDisplay'

const Gallery = () => {
  return (
      <div>
        <PictureDisplay />
      </div>
  )
}

export default Gallery
